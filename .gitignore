__pycache__/

# matplotlib cache
**/.mplcache/

# numba cache (Hotspot/numba JIT)
**/.numba_cache/

EvoGeneX/

nongenomic-evolution-of-tumor-subclones/

scphytr.egg-info/

.DS_Store

# Large external datasets (KP-Tracer etc.)
data/external/
# EvoGeneX bridge scratch (regenerated per run)
analysis/benchmark/evogenex_work/

# LaTeX build artifacts
paper/*.pdf
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.synctex.gz

# Re-fetchable SCOUT assets: the clone, their shipped data, and what extract_luad.R unpacks
analysis/scout/external/
analysis/scout/data/

# LaTeX build artefacts alongside the .tex sources in analysis/ (the .tex and .pdf
# are tracked on purpose; the rest is build junk)
analysis/**/*.aux
analysis/**/*.log
analysis/**/*.out
analysis/**/*.synctex.gz
analysis/**/*.fls
analysis/**/*.fdb_latexmk
